/* This component renders each of the
"warning" alerts to the Dashboard
Parent: Alerts */

import React from "react"

const Red = (props) => {

    return (
        <p>Red</p>
    )
}

export default Red